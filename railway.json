{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd packages/shared && npm run build && cd ../../apps/web && npm run build && cd ../api && npm run build"
  },
  "deploy": {
    "startCommand": "node apps/api/dist/db/migrate.js && node apps/api/dist/app.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
